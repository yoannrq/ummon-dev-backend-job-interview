import { AppDataSource } from "../data-source";
import { Color } from "../entities/Color";

export const ColorResolver = {
  // parts: async (color: Color, _variables: {}) => {
  //   return await AppDataSource.manager.findOneBy(Part, {
  //     color_id: color.id,
  //   });
  // },
};
